/**
 * Loading component for the product page.
 * This is a Next.js loading component that will be shown while the product page is loading.
 * Currently returns null as the product page handles its own loading state.
 *
 * @returns A React component that renders nothing (null)
 */
export default function Loading() {
  return null;
}
